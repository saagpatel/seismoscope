# App Store Connect API key credentials
# Copy to fastlane/.env (gitignored) and fill in your values.
# The .p8 key file must be placed separately (never commit it).

ASC_KEY_ID=
ASC_ISSUER_ID=
ASC_KEY_FILEPATH=
